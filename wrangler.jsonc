

{
  "name": "tehfiz",           // ← change to your actual Worker name (same as on main)
  "compatibility_date": "2026-04-14",   // ← today's date is fine, or copy whatever is on main
  "assets": {
    "directory": "./dist"               // ← change this to wherever your static files live
                                        // Common values: "./dist", "./public", "./build", or "./" if files are in root
  }
}
